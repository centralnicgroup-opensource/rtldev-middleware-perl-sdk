name: Release
on:
  push:
    branches:
      - master

jobs:
  release:
    uses: centralnicgroup-opensource/rtldev-middleware-shareable-workflows/.github/workflows/perl-sdk-release.yml@main
    secrets: inherit
